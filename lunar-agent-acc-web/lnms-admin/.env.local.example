# backend/.env 의 PORT 와 동일한 호스트로 맞추세요. 기본 미설정 시 admin 은 localhost:60000 을 호출합니다.
# 예: 백엔드가 PORT=58001 이면 아래처럼 두고, 이 파일을 .env.local 로 복사한 뒤 next dev / next build 하세요.
NEXT_PUBLIC_LNSMS_API=http://localhost:58001
